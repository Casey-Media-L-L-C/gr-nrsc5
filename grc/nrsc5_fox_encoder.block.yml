# auto-generated by grc.converter

id: nrsc5_fox_encoder
label: FOX encoder
category: '[NRSC-5]'

parameters:
-   id: long_name
    label: Long Station name
    dtype: string
    default: ABCD BOOP

outputs:
-   domain: stream
    dtype: byte
    vlen: 80
asserts:
- ${ len(long_name) }

templates:
    imports: import nrsc5
    make: nrsc5.fox_encoder(${long_name})

file_format: 1
